<cu-custom bgColor="bg-gradual-main-top" isBack="{{true}}">
	<view slot="backText">返回</view>
	<view slot="content">分享</view>
</cu-custom>
<image src="{{ img }}" style="height:{{height}}" bindlongpress="downloadImg"></image>
